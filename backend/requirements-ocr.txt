# Optional OCR fallback dependencies.
#
# Keep these outside requirements.txt because PaddleOCR/PaddlePaddle are large
# and only needed when OCR_PROVIDER=paddle is enabled.
paddlepaddle>=3.3.1,<3.4.0
paddleocr>=3.5.0,<3.6.0
